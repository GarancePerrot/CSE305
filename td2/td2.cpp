#pragma once
#include <cfloat>
#include <climits>
#include <thread>
#include <numeric>
#include <iterator>
#include <vector>


/**
 * @brief Computes an alternating sum of the elements in the array arr,
 * that is arr[0] - arr[1] + arr[2] - ...
 * @param arr - the array of integers
 * @param num_threads the number of threads to be used for computation
 */
int AltSumParallel(const std::vector<int>& arr, size_t num_threads) {
    // YOUR CODE HERE
    return 0;
}

//-----------------------------------------------------------------------------


